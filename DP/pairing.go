package DP

/*given a set of friends we have to find the number of
possibilities of pairing them up, provided they can
also remain single*/

func itrCount(array []int, len, e int) int {
	/*if len-e < 2 {
		return 1
	}

	return e*itrCount(array, len, e+1) + itrCount(array, len, e)*/
	return 0
}

/*the function takes an array(which represents friends) as
input. It returns an int, which is the number of ways
of pairing up these people*/
func countPairs(array []int) int {
	/*exclude := 0
	return itrCountPairs(array, len(array), exclude, count)*/
	return 0
}
