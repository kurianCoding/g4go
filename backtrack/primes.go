package backtrack

/* given a sum and a limit, the function will return a set of numbers
that are greater than the limit and whose sum is equal to the sum*/

func PrimeSum(a, b, c int) [][]int {
	return [][]int{{}}
}
